#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
#include <map>
#include <array>

using namespace std;

int S=1; int I=1; int P=0;
int ans= 0;
vector<int> STR; vector<int> INT; vector<int> PNT;

int main() {
	
	int N;
	cin >> N;
	int a, b, c;
	for(int i=0; i<N; i++){
		cin >> a >> b >> c;
		STR.push_back(a);
		INT.push_back(b);
		PNT.push_back(c);
	}
	
	
	
}