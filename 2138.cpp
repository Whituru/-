#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
#include <map>
#include <array>

using namespace std;

void flip(int i){
	if()
}

int main() {	
	ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
	
	int N; cin >> N;
	vector<int> cur;
	vector<int> goal;
	
	int tmp;
	for(int i=0; i<N; i++) {
		cin >> tmp;
		cur.push_back(tmp);
	}
	for(int i=0; i<N; i++){
		cin >> tmp;
		goal.push_back(tmp);
	}
	
	
	
	
	return 0;
	
}